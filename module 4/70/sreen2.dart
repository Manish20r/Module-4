import 'package:flutter/material.dart';

class Sreen2 extends StatelessWidget {
  const Sreen2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("gallery"),
    );
  }
}